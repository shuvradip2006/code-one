def fibonacci_series(n):
    series = []
    a, b = 0, 1
    for _ in range(n):
        series.append(a)
        a, b = b, a + b
    return series

# Example: Generate first 10 Fibonacci numbers
print(fibonacci_series(10))
